% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibration.R
\name{autoCalibrateAccel}
\alias{autoCalibrateAccel}
\title{Gravity-based accelerometer auto-calibration}
\usage{
autoCalibrateAccel(
  accel,
  sampling_rate,
  window_sec = 10,
  still_sd = 0.013,
  min_still = 10,
  max_iter = 100L,
  tol = 1e-06
)
}
\arguments{
\item{accel}{An n x 3 matrix of acceleration in g units.}

\item{sampling_rate}{Sampling rate in Hz.}

\item{window_sec}{Still-window length in seconds (default 10).}

\item{still_sd}{Per-axis standard-deviation threshold for a still window
(default 0.013 g).}

\item{min_still}{Minimum number of still windows required (default 10).}

\item{max_iter, tol}{Iteration cap and convergence tolerance.}
}
\value{
An \code{accel_calibration} list with \code{scale}, \code{offset}, \code{residual} (RMS of
the still-point magnitude error after calibration), \code{n_still},
\code{iterations}, \code{converged}, and a \code{calibrate()} function that applies the
correction to an n x 3 matrix.
}
\description{
Estimates a per-axis gain and offset that force the acceleration magnitude to
1 g during still periods, following the GGIR sphere-fitting method (van Hees
et al. 2014). Still (non-movement) windows are identified by a low per-axis
standard deviation; their mean orientations lie on a unit sphere in an
ideally-calibrated device, and the gain/offset are fitted by iteratively
projecting onto that sphere. Correction is \code{(accel - offset) * scale}.
}
\references{
van Hees VT, et al. (2014). J Appl Physiol 117(7):738-744.
}
\seealso{
\code{\link[=computeENMO]{computeENMO()}}
}
