% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sleep.R
\name{coleKripke}
\alias{coleKripke}
\title{Cole-Kripke actigraphy sleep/wake scoring}
\usage{
coleKripke(counts, rescore = TRUE)
}
\arguments{
\item{counts}{Numeric vector of per-epoch activity counts (one per minute).}

\item{rescore}{If \code{TRUE} (default) apply the Webster rescoring rules that flip
short isolated sleep bouts after sustained wake to wake.}
}
\value{
An integer vector the same length as \code{counts}: \code{1} = sleep, \code{0} = wake.
}
\description{
Scores each epoch as sleep or wake from per-epoch activity counts using the
Cole-Kripke algorithm: a weighted sum of the surrounding epochs' activity is
thresholded (\code{D < 1} => sleep). Weights are the classic 1-minute set.
}
\examples{
set.seed(1)
counts <- c(rpois(30, 40), rpois(60, 2), rpois(20, 45))  # active, quiet, active
table(coleKripke(counts))
}
\references{
Cole RJ, Kripke DF, Gruen W, Mullaney DJ, Gillin JC (1992).
"Automatic sleep/wake identification from wrist activity." Sleep 15:461-469.
}
\seealso{
\code{\link[=summarizeSleep]{summarizeSleep()}}, \code{\link[=summarizeAppleSleep]{summarizeAppleSleep()}}
}
