% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enmo.R
\name{computeENMOa}
\alias{computeENMOa}
\title{ENMO-abs (ENMOa) acceleration metric}
\usage{
computeENMOa(
  accel,
  sampling_rate = NULL,
  epoch_sec = NULL,
  unit = c("g", "mg")
)
}
\arguments{
\item{accel}{An n x 3 matrix (or data frame) of acceleration in g units
(columns x, y, z).}

\item{sampling_rate}{Sampling rate in Hz (required if \code{epoch_sec} is set).}

\item{epoch_sec}{Epoch length in seconds for aggregation; \code{NULL} returns the
per-sample ENMOa.}

\item{unit}{\code{"g"} (default) or \code{"mg"} (milli-g).}
}
\value{
A numeric vector of ENMOa values (per sample, or per epoch).
}
\description{
ENMOa is the "always-positive" companion of \code{\link{computeENMO}}: the
vector magnitude minus one gravity, with the \emph{absolute value} taken
instead of truncating negatives to zero, \eqn{{\rm ENMOa} = |r - 1|} with
\eqn{r = \sqrt{x^2+y^2+z^2}} (van Hees et al. 2013). It differs from ENMO only
where the magnitude dips below 1 g (deceleration / partial free-fall), which
ENMO truncates away; ENMOa retains that movement, so it is used when the
sensor calibration is imperfect (a persistent sub-1 g offset would otherwise
be lost by ENMO's truncation). When an epoch length is given, per-epoch mean
ENMOa is returned.
}
\examples{
set.seed(1)
acc <- matrix(rnorm(300, c(0, 0, 1), 0.1), ncol = 3, byrow = TRUE)
mean(computeENMOa(acc))          # >= mean(computeENMO): retains sub-1 g dips
}
\references{
van Hees VT, et al. (2013). PLoS ONE 8(4):e61691.
}
\seealso{
\code{\link[=computeENMO]{computeENMO()}}, \code{\link[=computeMAD]{computeMAD()}}
}
