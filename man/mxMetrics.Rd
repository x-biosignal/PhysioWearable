% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/freeliving-summary.R
\name{mxMetrics}
\alias{mxMetrics}
\title{MX physical-activity intensity metrics}
\usage{
mxMetrics(enmo_mg, epoch_sec, minutes = c(2, 5, 15, 30, 60, 120))
}
\arguments{
\item{enmo_mg}{Per-epoch ENMO in milli-g.}

\item{epoch_sec}{Epoch length in seconds.}

\item{minutes}{Integer vector of X values (minutes); default
\code{c(2, 5, 15, 30, 60, 120)}.}
}
\value{
A named numeric vector \code{Mxx = mg}; \code{NA} where the recording is shorter
than X minutes.
}
\description{
The MX metrics describe the same distribution from the active end: MX is the
acceleration (mg) above which a person's most active \code{X} minutes are
accumulated (Rowlands et al. 2018). M60 is the intensity of the most active
hour; M120 the most active two hours. Higher MX = more intense peak activity.
}
\examples{
# most-active 60 min all at >= 200 mg
enmo <- c(rep(300, 120), rep(20, 2000))   # 30 s epochs -> 120 = 60 min
mxMetrics(enmo, epoch_sec = 30, minutes = 60)
}
\references{
Rowlands AV, et al. (2018).
}
\seealso{
\code{\link[=intensityGradient]{intensityGradient()}}, \code{\link[=summarizeFreeLiving]{summarizeFreeLiving()}}
}
