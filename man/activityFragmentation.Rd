% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/freeliving-summary.R
\name{activityFragmentation}
\alias{activityFragmentation}
\title{Physical-activity fragmentation}
\usage{
activityFragmentation(intensity, epoch_sec)
}
\arguments{
\item{intensity}{A per-epoch intensity factor (from \code{\link[=classifyBouts]{classifyBouts()}}) or a
logical/0-1 vector marking active epochs (TRUE = active). When a factor is
given, any level other than \code{"sedentary"} counts as active.}

\item{epoch_sec}{Epoch length in seconds.}
}
\value{
A list of fragmentation metrics (durations in minutes).
}
\description{
Fragmentation metrics describe how activity and rest are \emph{accumulated} --
many short bouts (fragmented) versus few long ones. Reported for both the
active and sedentary states: mean bout duration, bout counts, the
transition probabilities (active-to-sedentary \code{astp}, sedentary-to-active
\code{satp}; each the reciprocal of the mean bout length in the origin state,
Chastin et al. 2010) and the Gini index of sedentary-bout durations.
}
\examples{
# alternating 4-epoch active / 6-epoch sedentary blocks
act <- rep(rep(c(TRUE, FALSE), c(4, 6)), 5)
activityFragmentation(act, epoch_sec = 60)$astp   # 1/4 = 0.25
}
\references{
Chastin SFM, Granat MH (2010). Gait Posture 31(1):82-86.
}
\seealso{
\code{\link[=summarizeFreeLiving]{summarizeFreeLiving()}}
}
