% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/har-adl.R
\name{adlFeatures}
\alias{adlFeatures}
\title{Windowed accelerometer features for activity recognition}
\usage{
adlFeatures(accel, sampling_rate, window_sec = 2.56, overlap = 0.5)
}
\arguments{
\item{accel}{An n x 3 acceleration matrix (g units; columns x, y, z).}

\item{sampling_rate}{Sampling rate in Hz.}

\item{window_sec}{Window length in seconds (default 2.56, the UCI-HAR value).}

\item{overlap}{Window overlap fraction in \verb{[0, 1)} (default 0.5).}
}
\value{
A data.frame with \code{window}, \code{start_sec}, \code{end_sec} and one column per
feature (one row per window).
}
\description{
Splits a raw tri-axial signal into fixed overlapping windows and computes, per
window, the compact time- and frequency-domain feature set used for human
activity recognition (per-axis and vector-magnitude mean/sd/MAD/energy/IQR/
range, the signal-magnitude area, inter-axis correlations, the dominant
frequency, spectral energy and entropy, and jerk RMS). Dependency-free.
}
\examples{
fs <- 20; t <- seq(0, 20, 1 / fs)
walk <- cbind(0.1 * sin(2 * pi * 1.8 * t), 0.1 * cos(2 * pi * 1.8 * t), 1)
head(adlFeatures(walk, fs), 3)
}
\references{
Anguita D, et al. (2013). ESANN.
}
\seealso{
\code{\link[=trainADL]{trainADL()}}, \code{\link[=recognizeADL]{recognizeADL()}}
}
