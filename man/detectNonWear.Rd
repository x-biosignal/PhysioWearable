% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibration.R
\name{detectNonWear}
\alias{detectNonWear}
\title{Detect non-wear periods}
\usage{
detectNonWear(
  accel,
  sampling_rate,
  window_min = 60,
  step_min = NULL,
  sd_thresh = 0.013,
  range_thresh = 0.05,
  min_axes = 2L
)
}
\arguments{
\item{accel}{An n x 3 matrix of acceleration in g units.}

\item{sampling_rate}{Sampling rate in Hz.}

\item{window_min}{Window length in minutes (default 60).}

\item{step_min}{Window step in minutes; \code{NULL} uses \code{window_min / 4}. A
sliding (overlapping) window prevents a non-wear period from being diluted
across a block boundary.}

\item{sd_thresh}{Per-axis standard-deviation threshold (default 0.013 g).}

\item{range_thresh}{Per-axis value-range threshold (default 0.050 g).}

\item{min_axes}{Number of axes that must meet both criteria (default 2).}
}
\value{
A \code{nonwear} list with a per-window \code{windows} data frame
(\code{window}, \code{start_time}, \code{end_time}, \code{nonwear}) and the \code{nonwear_fraction}
(fraction of samples covered by any non-wear window).
}
\description{
Flags non-wear windows where the accelerometer is stationary: at least
\code{min_axes} of the three axes have a low standard deviation and value range
over a long window (van Hees et al. 2011).
}
\references{
van Hees VT, et al. (2011). PLoS ONE 6(7):e22922.
}
