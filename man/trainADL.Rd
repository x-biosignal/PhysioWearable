% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/har-adl.R
\name{trainADL}
\alias{trainADL}
\alias{predict.adl_model}
\title{Train an ADL activity recogniser from labelled windows}
\usage{
trainADL(features, labels, k = 5, window_sec = 2.56, overlap = 0.5)

\method{predict}{adl_model}(object, newdata, ...)
}
\arguments{
\item{features}{Training features: an \code{\link[=adlFeatures]{adlFeatures()}} data.frame or a numeric
feature matrix (one row per window).}

\item{labels}{Activity label per window (length = number of windows).}

\item{k}{Neighbours for the vote (default 5).}

\item{window_sec, overlap}{Window settings to store so \code{\link[=recognizeADL]{recognizeADL()}} can
re-extract features consistently (defaults match \code{\link[=adlFeatures]{adlFeatures()}}).}

\item{object}{an \code{adl_model}.}

\item{newdata}{features to classify (\code{\link[=adlFeatures]{adlFeatures()}} data.frame or matrix).}

\item{...}{unused.}
}
\value{
an \code{adl_model} object.
}
\description{
Learns a k-nearest-neighbours classifier over standardised \code{\link[=adlFeatures]{adlFeatures()}},
mapping a window's features to an activity label. Dependency-free and
model-agnostic: for a stronger engine, extract features here and classify with
PhysioML (ROCKET/torch) instead -- the ICF mapping (\code{\link[=adlToICF]{adlToICF()}}) is unchanged.
}
\examples{
set.seed(1)
fs <- 20; t <- seq(0, 30, 1 / fs)
walk <- cbind(0.2 * sin(2 * pi * 1.8 * t), 0.2 * cos(2 * pi * 1.8 * t), 1)
sit  <- cbind(rnorm(length(t), 0, 0.01), rnorm(length(t), 0, 0.01), 1)
fw <- adlFeatures(walk, fs); fs2 <- adlFeatures(sit, fs)
feats <- rbind(fw, fs2)
labs <- c(rep("walking", nrow(fw)), rep("sitting", nrow(fs2)))
m <- trainADL(feats, labs)
m
}
\seealso{
\code{\link[=recognizeADL]{recognizeADL()}}, \code{\link[=adlFeatures]{adlFeatures()}}, \code{\link[=adlToICF]{adlToICF()}}
}
