% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spo2.R
\name{spo2Metrics}
\alias{spo2Metrics}
\title{Oximetry (SpO2) metrics}
\usage{
spo2Metrics(spo2, time = NULL, threshold = 90, desat = 3, window = 12)
}
\arguments{
\item{spo2}{Numeric SpO2 series (percent or 0-1 fraction).}

\item{time}{Optional sample times as \code{POSIXct} or numeric seconds (same length
and order as \code{spo2}). Enables \code{t90_min} and per-hour \code{odi}.}

\item{threshold}{Desaturation threshold percent for T90/CT90 (default 90).}

\item{desat}{Drop (percentage points) below the rolling baseline that defines
a desaturation event (default 3; 4 is also common).}

\item{window}{Rolling-baseline length in samples for event detection (default 12).}
}
\value{
An \code{spo2_metrics} object: \code{mean}, \code{nadir}, \code{pct_below}, \code{ct90},
\code{t90_min}, \code{n_desat}, \code{odi} (events/hour), plus the settings.
}
\description{
Computes the standard blood-oxygen summary indices from an SpO2 series.
Accepts SpO2 as a percentage (90) or a fraction (0.90; auto-detected and
scaled). When \code{time} is supplied the time-below-threshold and desaturation
index are duration-weighted; otherwise they are sample proportions/counts.
}
\examples{
set.seed(1)
spo2 <- pmin(100, pmax(80, 97 + round(rnorm(200))))
spo2[50:55] <- 88   # a desaturation
spo2Metrics(spo2)
}
\references{
Chung F et al. (2012) oximetry desaturation indices; standard
ODI/CT90 definitions used in sleep-disordered-breathing screening.
}
\seealso{
\code{\link[=summarizeAppleSleep]{summarizeAppleSleep()}}
}
