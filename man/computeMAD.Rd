% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enmo.R
\name{computeMAD}
\alias{computeMAD}
\title{Mean Amplitude Deviation (MAD) acceleration metric}
\usage{
computeMAD(accel, sampling_rate = NULL, epoch_sec = NULL, unit = c("g", "mg"))
}
\arguments{
\item{accel}{An n x 3 matrix (or data frame) of acceleration in g units
(columns x, y, z).}

\item{sampling_rate}{Sampling rate in Hz (required if \code{epoch_sec} is set).}

\item{epoch_sec}{Epoch length in seconds for aggregation; \code{NULL} returns a
single MAD over the whole signal.}

\item{unit}{\code{"g"} (default) or \code{"mg"} (milli-g).}
}
\value{
A numeric vector of MAD values (one per epoch, or a single value).
}
\description{
MAD is the mean absolute deviation of the tri-axial acceleration vector
magnitude about its (per-epoch) mean (Vaha-Ypya et al. 2015): for each epoch,
\eqn{{\rm MAD} = \frac{1}{n}\sum_i |r_i - \bar r|}, where
\eqn{r_i = \sqrt{x_i^2 + y_i^2 + z_i^2}} is the sample vector magnitude and
\eqn{\bar r} its epoch mean. It is a gravity-robust movement-intensity metric
complementary to \code{\link{computeENMO}}: MAD captures the amount of
\emph{variation} in the magnitude within an epoch (so it is insensitive to a
static orientation offset), whereas ENMO captures its \emph{elevation} above
1 g. When an epoch length is given, per-epoch MAD is returned; otherwise a
single whole-signal MAD.
}
\examples{
set.seed(1)
acc <- matrix(rnorm(300, c(0, 0, 1), 0.1), ncol = 3, byrow = TRUE)
computeMAD(acc)                       # whole-signal MAD (g)
computeMAD(acc, sampling_rate = 100, epoch_sec = 1)  # per-epoch MAD
}
\references{
Vaha-Ypya H, et al. (2015). Clin Physiol Funct Imaging 35(1):64-70.
}
\seealso{
\code{\link[=computeENMO]{computeENMO()}}, \code{\link[=classifyBouts]{classifyBouts()}}
}
