% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enmo.R
\name{computeENMO}
\alias{computeENMO}
\title{Euclidean Norm Minus One (ENMO) acceleration metric}
\usage{
computeENMO(accel, sampling_rate = NULL, epoch_sec = NULL, unit = c("g", "mg"))
}
\arguments{
\item{accel}{An n x 3 matrix (or data frame) of acceleration in g units
(columns x, y, z).}

\item{sampling_rate}{Sampling rate in Hz (required if \code{epoch_sec} is set).}

\item{epoch_sec}{Epoch length in seconds for aggregation; \code{NULL} returns the
per-sample ENMO.}

\item{unit}{\code{"g"} (default) or \code{"mg"} (milli-g).}
}
\value{
A numeric vector of ENMO values (per sample, or per epoch).
}
\description{
ENMO is the vector magnitude of tri-axial acceleration minus one gravity,
with negative values truncated to zero (van Hees et al. 2013). For a
perfectly still sensor (magnitude 1 g) ENMO is 0; movement raises it. When an
epoch length is given, per-epoch mean ENMO is returned.
}
\examples{
still <- matrix(rep(c(0, 0, 1), each = 100), ncol = 3)
max(computeENMO(still))          # ~ 0 for a static 1 g signal
}
\references{
van Hees VT, et al. (2013). PLoS ONE 8(4):e61691.
}
\seealso{
\code{\link[=classifyBouts]{classifyBouts()}}, \code{\link[=autoCalibrateAccel]{autoCalibrateAccel()}}
}
