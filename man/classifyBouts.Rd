% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enmo.R
\name{classifyBouts}
\alias{classifyBouts}
\title{Classify physical-activity intensity and detect activity bouts}
\usage{
classifyBouts(
  enmo,
  epoch_sec,
  thresholds = paIntensityThresholds(),
  enmo_unit = c("mg", "g"),
  bout_level = c("moderate", "light", "vigorous"),
  min_bout_min = 1
)
}
\arguments{
\item{enmo}{Per-epoch ENMO values (from \code{\link[=computeENMO]{computeENMO()}}).}

\item{epoch_sec}{Epoch length in seconds.}

\item{thresholds}{Named intensity thresholds in mg (see
\code{\link[=paIntensityThresholds]{paIntensityThresholds()}}).}

\item{enmo_unit}{Unit of \code{enmo}: \code{"mg"} (default) or \code{"g"}.}

\item{bout_level}{Minimum intensity for a bout: \code{"light"}, \code{"moderate"}
(default) or \code{"vigorous"}.}

\item{min_bout_min}{Minimum bout duration in minutes (default 1).}
}
\value{
A \code{wearable_bouts} list with \code{intensity} (per-epoch factor), a \code{bouts}
data frame (\code{start_epoch}, \code{end_epoch}, \code{n_epochs}, \code{duration_sec},
\code{mean_enmo}), and \code{minutes} per intensity.
}
\description{
Classifies each ENMO epoch into a physical-activity intensity (sedentary,
light, moderate, vigorous) and detects contiguous bouts at or above a target
intensity lasting at least a minimum duration.
}
\references{
Hildebrand M, et al. (2014).
}
\seealso{
\code{\link[=computeENMO]{computeENMO()}}, \code{\link[=paIntensityThresholds]{paIntensityThresholds()}}
}
