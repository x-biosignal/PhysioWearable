% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sleep.R
\name{summarizeSleepStages}
\alias{summarizeSleepStages}
\title{Summarise staged sleep into per-night clinical metrics}
\usage{
summarizeSleepStages(
  stages,
  asleep_levels,
  wake_levels = "Awake",
  inbed_levels = "InBed",
  stage_cols = NULL,
  gap_min = 120
)
}
\arguments{
\item{stages}{A data frame with \code{start}, \code{end} (\code{POSIXct}) and a stage-label
column named \code{value} or \code{stage}.}

\item{asleep_levels}{Labels counted as asleep (e.g. Apple
\code{c("AsleepCore","AsleepDeep","AsleepREM")}; Fitbit \code{c("light","deep","rem")}).}

\item{wake_levels}{Labels counted as wake within the sleep period (default
\code{"Awake"}; Fitbit \code{"wake"}).}

\item{inbed_levels}{Labels counted as explicit time in bed (default \code{"InBed"}).}

\item{stage_cols}{Optional named character vector mapping output columns to
stage labels, e.g. \code{c(deep = "deep", rem = "rem")} adds \code{deep_min}, \code{rem_min}.}

\item{gap_min}{Minutes of gap that start a new night (default 120).}
}
\value{
A data frame, one row per night: \code{night}, \code{start}, \code{end}, \code{tib_min},
\code{tst_min}, \code{sleep_efficiency}, \code{waso_min}, \code{sol_min}, \code{n_awakenings}, and one
\verb{<name>_min} column per \code{stage_cols} entry.
}
\description{
Vendor-agnostic engine behind \code{\link[=summarizeAppleSleep]{summarizeAppleSleep()}}: given sleep-stage
intervals labelled by any scheme (Apple \code{AsleepCore/Deep/REM}, Fitbit
\code{light/deep/rem/wake}, ...), compute per-night sleep metrics. Consecutive
intervals are grouped into nights whenever the gap between them exceeds
\code{gap_min}.
}
\examples{
t0 <- as.POSIXct("2023-05-01 23:00:00", tz = "UTC")
stages <- data.frame(start = t0 + c(0, 3600, 7200),
                     end = t0 + c(3600, 7200, 10800),
                     stage = c("light", "deep", "rem"))
summarizeSleepStages(stages, asleep_levels = c("light", "deep", "rem"),
                     wake_levels = "wake", stage_cols = c(deep = "deep", rem = "rem"))
}
\seealso{
\code{\link[=summarizeAppleSleep]{summarizeAppleSleep()}}, \code{\link[=summarizeSleep]{summarizeSleep()}}, \code{\link[=coleKripke]{coleKripke()}}
}
