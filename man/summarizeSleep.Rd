% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sleep.R
\name{summarizeSleep}
\alias{summarizeSleep}
\title{Summarise a sleep/wake series into standard sleep metrics}
\usage{
summarizeSleep(sleep_wake, epoch = 60, spt = NULL)
}
\arguments{
\item{sleep_wake}{Integer/logical per-epoch sleep(1)/wake(0) vector (e.g. from
\code{\link[=coleKripke]{coleKripke()}}).}

\item{epoch}{Epoch length in seconds (default 60).}

\item{spt}{Optional integer length-2 vector \code{c(start, end)} of epoch indices
bounding the sleep-period (time in bed). If \code{NULL} (default) it spans the
first to the last sleep epoch.}
}
\value{
A one-row data frame: \code{tib_min} (time in bed), \code{tst_min} (total sleep
time), \code{sleep_efficiency} (TST/TIB), \code{waso_min} (wake after sleep onset),
\code{sol_min} (sleep-onset latency), \code{n_awakenings}.
}
\description{
Summarise a sleep/wake series into standard sleep metrics
}
\examples{
sw <- c(rep(0, 5), rep(1, 40), 0, 0, rep(1, 30))
summarizeSleep(sw)
}
\seealso{
\code{\link[=coleKripke]{coleKripke()}}
}
