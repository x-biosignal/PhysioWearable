% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/icf-link.R
\name{freeLivingICF}
\alias{freeLivingICF}
\title{Map free-living metrics to ICF codes (performance qualifier)}
\usage{
freeLivingICF(x = NULL, hub = NULL)
}
\arguments{
\item{x}{A \code{freeliving_summary} (from \code{\link[=summarizeFreeLiving]{summarizeFreeLiving()}}) or a character
vector of metric ids; \code{NULL} (default) returns the full free-living map.}

\item{hub}{Optional \code{PhysioAnnotationHub} object passed through to the
ontology lookup.}
}
\value{
A data frame with \code{metric}, \code{icf_code}, \code{icf_title}, \code{qualifier}
(always \code{"performance"}) and, for a summary input, \code{value}.
}
\description{
Returns the ICF Activities & Participation codes for free-living
physical-behaviour metrics, each labelled with the ICF \code{performance}
qualifier -- the distinguishing feature of free-living monitoring, which
captures what a person actually does in daily life rather than the
\code{capacity} a clinic test measures. When a \code{\link[=summarizeFreeLiving]{summarizeFreeLiving()}} result is
supplied, each mapped metric's aggregate value is attached.
}
\details{
Uses the maintained ontology in the suggested package
\pkg{PhysioAnnotationHub} when available (its free-living rows), falling back
to a built-in table otherwise.
}
\examples{
freeLivingICF()                       # the free-living metric -> ICF map
freeLivingICF(c("mvpa_min", "steps_per_day"))
}
\seealso{
\code{\link[=summarizeFreeLiving]{summarizeFreeLiving()}}
}
