% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/freeliving-summary.R
\name{summarizeFreeLiving}
\alias{summarizeFreeLiving}
\title{Summarise free-living physical behaviour over one or more days}
\usage{
summarizeFreeLiving(
  enmo,
  epoch_sec,
  timestamps = NULL,
  wear = NULL,
  enmo_unit = c("mg", "g"),
  thresholds = paIntensityThresholds(),
  valid_wear_hours = 10,
  steps_per_day = NULL,
  guideline_mvpa_min = 150
)
}
\arguments{
\item{enmo}{Per-epoch ENMO (see \code{\link[=computeENMO]{computeENMO()}}).}

\item{epoch_sec}{Epoch length in seconds.}

\item{timestamps}{Optional per-epoch \code{POSIXct} timestamps; when supplied, days
are split on the calendar date and the diurnal profile is computed.}

\item{wear}{Optional per-epoch logical wear mask (\code{TRUE} = worn). Default: all
worn.}

\item{enmo_unit}{Unit of \code{enmo}: \code{"mg"} (default) or \code{"g"}.}

\item{thresholds}{Intensity cut-points in mg (see \code{\link[=paIntensityThresholds]{paIntensityThresholds()}}).}

\item{valid_wear_hours}{Minimum wear hours for a day to be valid (default 10).}

\item{steps_per_day}{Optional numeric vector of steps for each day (in day
order) folded into the per-day table and averages.}

\item{guideline_mvpa_min}{Weekly MVPA-equivalent minutes for guideline
attainment (default 150; WHO 2020, counting vigorous double).}
}
\value{
A \code{freeliving_summary} object: \code{by_day} (per-day data frame),
\code{aggregate} (means over valid days plus guideline attainment), \code{diurnal}
(or \code{NULL}), \code{n_valid_days} and settings.
}
\description{
The umbrella summary: from multi-day per-epoch ENMO (plus optional timestamps
and a wear mask) it computes, per valid day and averaged across valid days,
the physical-behaviour metrics that free-living accelerometry studies report
-- intensity time-use (sedentary / light / MVPA), activity volume, the
Rowlands intensity gradient and MX metrics, activity fragmentation, the
diurnal profile and WHO guideline attainment. Only days with enough wear time
are counted as valid; non-wear epochs are excluded from the intensity tallies.
}
\examples{
set.seed(1)
# one day, 30 s epochs: mostly sedentary with an active hour
enmo <- c(rep(5, 2 * 60 * 10), rep(120, 2 * 60), rep(5, 2 * 60 * 12))
s <- summarizeFreeLiving(enmo, epoch_sec = 30, valid_wear_hours = 0)
s$aggregate$mvpa_min
}
\references{
Rowlands (2018); Chastin (2010); WHO (2020).
}
\seealso{
\code{\link[=intensityGradient]{intensityGradient()}}, \code{\link[=mxMetrics]{mxMetrics()}}, \code{\link[=activityFragmentation]{activityFragmentation()}},
\code{\link[=freeLivingICF]{freeLivingICF()}}
}
